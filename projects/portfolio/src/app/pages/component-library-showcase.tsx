import ComponentLibraryIndex from "@/component-library/IndexPage";

/**
 * Full component library showcase — all demo pages migrated from
 * ld-kit-agent-kits. Reached at /component-library with hash-based nav
 * (e.g. /component-library#components-button).
 */
export function ComponentLibraryShowcase() {
  return <ComponentLibraryIndex />;
}
