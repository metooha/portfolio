import LivingDesignIndex from "@/living-design-showcase/pages/IndexPage";

/**
 * Full Living Design component library — all demo pages migrated from
 * ld-kit-agent-kits. Reached at /living-design with hash-based nav
 * (e.g. /living-design#components-button).
 */
export function LivingDesignShowcase() {
  return <LivingDesignIndex />;
}
